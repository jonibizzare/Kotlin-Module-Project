import java.util.Scanner

class UserInput {
    fun menuInput () : String {
        val sc = Scanner(System.`in`).nextLine()
        return sc
    }
    fun textInput () : String {
        val sc = Scanner(System.`in`).nextLine()
        return sc
    }
}